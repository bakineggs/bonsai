# A node can be transformed if there is a rule that can transform it
...*:
  -Node: N
  +Node: T
Rules:
  Rule:
    Transformation:
      From: N
      To: T

...*::
  -Node: N
  +Node: T
Rules:
  Rule:
    Transformation:
      From: N
      To: T
